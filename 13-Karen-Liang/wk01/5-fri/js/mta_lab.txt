// // Create a program that models a simple subway system.

// The program takes the line and stop that a user is getting on at and the line and stop that user is getting off at and prints the journey and the total number of stops for the trip in the console:

// planTrip('N', 'Times Square', '6', '33rd'); // This is only a suggested function name and signature.

// console.log() shows output similar to this:


//  Tell the user the number of stops AND the stops IN ORDER that they will pass through or change at.

    // "You must travel through the following stops on the N line: 34th, 28th, 23rd, Union Square."

    // "Change at Union Square."
    //  "Your journey continues through the following stops: 23rd, 28th, 33rd."
    // "7 stops in total."


let line = {
    n: {
        trainStop: ['Times Square', '34th', '28th', '23rd', 'Union Square', '8th'],
    },

    l: {
        trainStop: ['8th', '6th', 'Union Square', '3rd', '1st'],
    },

    six: { //6Line
        tranStop: ['Grand Central', '33rd', '28th', '23', 'Union Square', 'Astor Place'],
    }
};

// nLine = ['Times Square = 0 ', '34th - 1', '28th - 2', '23rd - 3 ', 'Union Square- 4, '8th - 5'],

const planTrip = function (startLine,tripA, changeLine, tripB) { // add linename in here eventually to console.log it 
    
    // index of starting point
    
    let resultA = line.n.trainStop.indexOf(tripA);
    // console.log(resultA + 1); 

    // index of end point
    let resultB = line.n.trainStop.indexOf(tripB);
    // console.log(resultB + 1); 

    // train stops total in between A and B
    let stopsBetween = resultB - resultA
    let totalStops = Math.abs(stopsBetween); // // Math.abs negative to positive
    if (totalStops > 1) {
        console.log(`${ totalStops } stops in total`); // add s if >1 stop
    } else {
        console.log(`${ totalStops } stop in total`) 
    };
    

    let startIndex = resultA;
    let stopIndex = resultB

    if (startIndex < stopIndex) {
        console.log(`You must travel through the following stops on the ${ startLine }: ${line.n.trainStop.slice(resultA, resultB).join(', and ')}.`); // show stops travelling forward
    } else {
        console.log(`You must travel through the following stops on the ${ startLine }: ${line.n.trainStop.slice(resultB, resultA).join(', and ')}.`); // show stops travelling backward
    }

}


//  planTrip('N line', 'Times Square', ' ', '28th');
// // planTrip('N line', '23rd', ' ', '34th');
planTrip ('N', 'Times Square', '6', '33rd')


   // getting array # from resultA and B to log stops in between. 
   // for (i = 0; i< network.nLine.length; i++) {

